import {test} from 'node:test';import assert from 'node:assert/strict';import {cancellationStrategy} from '../lib/domain/cancellation.ts';import {serviceById} from '../lib/domain/catalog.ts';import type {Service} from '../lib/domain/types.ts';
test('cancellation strategy is independent of UI and uses verified metadata',()=>{assert.equal(cancellationStrategy(serviceById('yandex-plus')).type,'external-url');assert.equal(cancellationStrategy(undefined).type,'unsupported');const s:Service={id:'s',name:'s',category:'service',merchantAliases:[],color:'#000',monogram:'S',cancellationInstructions:['Обратитесь в поддержку.']};assert.deepEqual(cancellationStrategy(s),{type:'instructions',steps:s.cancellationInstructions});assert.equal(cancellationStrategy({...s,manageSubscriptionUrl:'javascript:bad',cancellationInstructions:[]}).type,'unsupported')});
