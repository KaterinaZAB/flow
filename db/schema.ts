import {sqliteTable,text,integer,real,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const users=sqliteTable('users',{id:text().primaryKey(),createdAt:text('created_at').notNull()});
export const expenses=sqliteTable('recurring_expenses',{id:text().primaryKey(),userId:text('user_id').notNull().references(()=>users.id,{onDelete:'cascade'}),name:text().notNull(),type:text().notNull(),amountMinor:integer('amount_minor').notNull(),currency:text().notNull(),billingPeriod:text('billing_period').notNull(),customDays:integer('custom_days'),nextPaymentAt:text('next_payment_at'),anchorDay:integer('anchor_day'),status:text().notNull(),serviceId:text('service_id'),source:text().notNull(),confidence:real(),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull()},t=>[index('idx_expenses_user_status').on(t.userId,t.status)]);
export const imports=sqliteTable('transaction_imports',{id:text().primaryKey(),userId:text('user_id').notNull().references(()=>users.id,{onDelete:'cascade'}),filename:text().notNull(),format:text().notNull(),status:text().notNull(),createdAt:text('created_at').notNull(),transactionCount:integer('transaction_count').notNull().default(0),skippedCount:integer('skipped_count').notNull().default(0),error:text()},t=>[index('idx_imports_user').on(t.userId)]);
export const transactions=sqliteTable('transactions',{id:text().primaryKey(),userId:text('user_id').notNull().references(()=>users.id,{onDelete:'cascade'}),originalMerchant:text('original_merchant').notNull(),normalizedMerchant:text('normalized_merchant').notNull(),amountMinor:integer('amount_minor').notNull(),currency:text().notNull(),paidAt:text('paid_at').notNull(),recurringExpenseId:text('recurring_expense_id').references(()=>expenses.id,{onDelete:'set null'}),sourceImportId:text('source_import_id').notNull().references(()=>imports.id,{onDelete:'cascade'}),fingerprint:text().notNull(),occurrence:integer().notNull().default(0)},t=>[uniqueIndex('idx_transactions_dedupe').on(t.userId,t.fingerprint,t.occurrence),index('idx_transactions_user_merchant').on(t.userId,t.normalizedMerchant),index('idx_transactions_expense').on(t.recurringExpenseId)]);
export const candidates=sqliteTable('detection_candidates',{id:text().primaryKey(),userId:text('user_id').notNull().references(()=>users.id,{onDelete:'cascade'}),importId:text('import_id').notNull().references(()=>imports.id,{onDelete:'cascade'}),payload:text().notNull(),decision:text().notNull().default('pending')},t=>[index('idx_candidates_user_decision').on(t.userId,t.decision)]);
