'use client';import {useEffect} from 'react';
type Context={registerTool:(tool:{name:string;title:string;description:string;inputSchema:object;annotations:object;execute:(input:unknown)=>Promise<unknown>},options:{signal:AbortSignal})=>void|Promise<void>};
export function WebMCP(){useEffect(()=>{const context=(document as Document&{modelContext?:Context}).modelContext;if(!context?.registerTool)return;const lifecycle=new AbortController();try{void Promise.resolve(context.registerTool({name:'potok_list_recurring_expenses',title:'Регулярные расходы Потока',description:'Прочитать список расходов текущего аккаунта с суммой в копейках, периодом и статусом. Требуется вход.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true,untrustedContentHint:true},execute:async(input)=>{if(!input||typeof input!=='object'||Array.isArray(input)||Object.keys(input).length)throw new Error('Ожидается пустой объект.');const r=await fetch('/api/expenses',{cache:'no-store'});if(!r.ok)throw new Error('Войдите в Поток, чтобы прочитать расходы.');return r.json()}},{signal:lifecycle.signal})).catch(()=>{})}catch{}return()=>lifecycle.abort()},[]);return null}
