import {serviceById} from '@/lib/domain/catalog';import {House,Wifi,Smartphone,Repeat2} from 'lucide-react';
export function ServiceLogo({name,serviceId,type,large=false}:{name:string;serviceId?:string|null;type?:string;large?:boolean}){const service=serviceById(serviceId);const Icon=type==='utility'||type==='rent'?House:type==='internet'?Wifi:type==='mobile'?Smartphone:Repeat2;return <span className={'service-logo '+(large?'large':'')} style={service?{background:service.color+'13',color:service.color}:undefined} aria-hidden="true">{service?service.monogram:<Icon size={large?29:19}/>}</span>}
